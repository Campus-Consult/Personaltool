@using Microsoft.AspNetCore.Identity
@using Personaltool.Models
<ul class="navbar-nav">
    @if (User.Identity.IsAuthenticated)
    {
        <li class="nav-item">
            <a class="nav-link text-dark" title="Manage">Hello @User.FindFirst(m => m.Type == "name").Value!</a><!-- TODO this should lead to the users personal profile -->
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-controller="Account" asp-action="Logout">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout<i class="fas ml-2 fa-sign-out-alt"></i></button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Account" asp-action="Login">Login<i class="fas ml-2 fa-sign-in-alt"></i></a>
        </li>
    }
</ul>
