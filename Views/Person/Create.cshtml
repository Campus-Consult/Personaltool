@model Personaltool.Models.Person

@{
    ViewData["Title"] = "Create";
}

<div class="container">
    <h1>Create</h1>
    @{await Html.RenderPartialAsync("_CreatePartial", Model);}
    <div>
        <a asp-action="Index">Back to List</a>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
